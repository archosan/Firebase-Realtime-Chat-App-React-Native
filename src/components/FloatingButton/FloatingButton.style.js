import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    backgroundColor: '#FFA040',
    borderRadius: 50,
    padding: 15,
    bottom: 10,
    right: 20,
    zIndex: 99,
  },
});
